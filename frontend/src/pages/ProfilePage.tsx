import {useEffect,useState} from 'react';import {useParams} from 'react-router-dom';import {authApi} from '../api/discussions';import type {Discussion,User} from '../types';import DiscussionList from '../components/DiscussionList';
export default function ProfilePage(){const {username=''}=useParams(),[profile,setProfile]=useState<(User&{discussions:Discussion[]})>();useEffect(()=>{authApi.profile(username).then(setProfile)},[username]);if(!profile)return <div className="state">Загрузка…</div>;return <main className="profile"><section><div className="profile-avatar">{profile.displayName[0]}</div><h1>{profile.displayName}</h1><b>@{profile.username}</b><p>{profile.bio||'Участник сообщества'}</p><small>На платформе с {new Date(profile.createdAt||'').toLocaleDateString('ru')}</small></section><div><h2>Идеи пользователя</h2><DiscussionList items={profile.discussions}/></div></main>}
