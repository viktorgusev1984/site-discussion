import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {authApi} from '../api/discussions';import {useAuth} from '../auth/AuthContext';
const DEMO_ACCOUNTS=[{label:'Пользователь',username:'demo',password:'demo12345'},{label:'Администратор',username:'admin',password:'admin12345'}] as const;
export default function AuthPage({register=false}:{register?:boolean}){const nav=useNavigate(),{signIn}=useAuth(),[form,setForm]=useState({username:'',displayName:'',email:'',password:''}),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{const data=register?await authApi.register(form):await authApi.login(form);signIn(data);nav('/')}catch{setError('Не удалось выполнить запрос. Проверьте данные.')}}function useDemo(username:string,password:string){setForm({...form,username,password});setError('')}return <main className="auth-page"><div className="auth-logo">◎</div><form className="auth-card" onSubmit={submit}><h1>{register?'Создать аккаунт':'С возвращением'}</h1><p>{register?'Присоединяйтесь к обсуждению':'Войдите, чтобы голосовать и отвечать'}</p>{!register&&<aside className="demo-account"><strong>Тестовые аккаунты</strong>{DEMO_ACCOUNTS.map(account=><div className="demo-account-row" key={account.username}><span>{account.label}: <code>{account.username}</code> · <code>{account.password}</code></span><button type="button" onClick={()=>useDemo(account.username,account.password)}>Войти как {account.label.toLowerCase()}</button></div>)}</aside>}{register&&<><label>Имя<input value={form.displayName} onChange={e=>setForm({...form,displayName:e.target.value})} required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label></>}<label>Логин<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required/></label><label>Пароль<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} minLength={8} required/></label>{error&&<div className="error">{error}</div>}<button className="primary">{register?'Зарегистрироваться':'Войти'}</button><small>{register?'Уже есть аккаунт?':'Нет аккаунта?'} <Link to={register?'/login':'/register'}>{register?'Войти':'Регистрация'}</Link></small></form></main>}
