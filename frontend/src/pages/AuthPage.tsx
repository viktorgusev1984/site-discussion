import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {authApi} from '../api/discussions';
const DEMO_CREDENTIALS={username:'demo',password:'demo12345'};
export default function AuthPage({register=false}:{register?:boolean}){const nav=useNavigate(),[form,setForm]=useState({username:'',displayName:'',email:'',password:''}),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{const data=register?await authApi.register(form):await authApi.login(form);localStorage.setItem('token',data.token);nav('/')}catch{setError('Не удалось выполнить запрос. Проверьте данные.')}}function useDemo(){setForm({...form,...DEMO_CREDENTIALS});setError('')}return <main className="auth-page"><div className="auth-logo">◎</div><form className="auth-card" onSubmit={submit}><h1>{register?'Создать аккаунт':'С возвращением'}</h1><p>{register?'Присоединяйтесь к обсуждению':'Войдите, чтобы голосовать и отвечать'}</p>{!register&&<aside className="demo-account"><strong>Тестовый аккаунт</strong><span>Логин: <code>demo</code> · Пароль: <code>demo12345</code></span><button type="button" onClick={useDemo}>Подставить данные</button></aside>}{register&&<><label>Имя<input value={form.displayName} onChange={e=>setForm({...form,displayName:e.target.value})} required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label></>}<label>Логин<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required/></label><label>Пароль<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} minLength={8} required/></label>{error&&<div className="error">{error}</div>}<button className="primary">{register?'Зарегистрироваться':'Войти'}</button><small>{register?'Уже есть аккаунт?':'Нет аккаунта?'} <Link to={register?'/login':'/register'}>{register?'Войти':'Регистрация'}</Link></small></form></main>}
