import {render,screen} from '@testing-library/react';import userEvent from '@testing-library/user-event';import {MemoryRouter} from 'react-router-dom';import {describe,it,expect,vi} from 'vitest';import DiscussionsPage from './DiscussionsPage';import {discussionsApi} from '../api/discussions';
vi.spyOn(discussionsApi,'categories').mockResolvedValue([]);const list=vi.spyOn(discussionsApi,'list').mockResolvedValue({content:[],number:0,totalPages:0,totalElements:0});describe('discussion filters',()=>{it('passes search to API',async()=>{render(<MemoryRouter><DiscussionsPage/></MemoryRouter>);await userEvent.type(screen.getByLabelText('Поиск'),'roadmap');expect(list).toHaveBeenLastCalledWith(expect.objectContaining({q:'roadmap'}))})})
