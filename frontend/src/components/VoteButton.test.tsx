import {render,screen} from '@testing-library/react';import userEvent from '@testing-library/user-event';import {describe,it,expect,vi} from 'vitest';import VoteButton from './VoteButton';import {discussionsApi} from '../api/discussions';
vi.spyOn(discussionsApi,'vote').mockResolvedValue({});describe('VoteButton',()=>{it('toggles the vote optimistically',async()=>{render(<VoteButton id={1} count={3}/>);const button=screen.getByRole('button');await userEvent.click(button);expect(button).toHaveAttribute('aria-pressed','true');expect(screen.getByText('4')).toBeInTheDocument()})})
