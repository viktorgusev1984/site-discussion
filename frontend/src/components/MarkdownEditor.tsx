import {useState} from 'react';import ReactMarkdown from 'react-markdown';
export default function MarkdownEditor({value,onChange,error}:{value:string;onChange:(v:string)=>void;error?:string}){const [preview,setPreview]=useState(false);return <div className={`editor ${error?'invalid':''}`}><div className="editor-tabs"><button type="button" className={!preview?'active':''} onClick={()=>setPreview(false)}>Написать</button><button type="button" className={preview?'active':''} onClick={()=>setPreview(true)}>Предпросмотр</button><span>Поддерживается Markdown</span></div>{preview?<div className="preview"><ReactMarkdown>{value||'*Здесь появится предпросмотр*'}</ReactMarkdown></div>:<textarea aria-label="Описание" value={value} onChange={e=>onChange(e.target.value)} placeholder="Подробно опишите идею…" rows={12}/>} {error&&<small className="error">{error}</small>}</div>}
