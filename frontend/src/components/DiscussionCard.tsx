import {Link} from 'react-router-dom';import type {Discussion} from '../types';import VoteButton from './VoteButton';
export default function DiscussionCard({item}:{item:Discussion}){return <article className="discussion-card"><VoteButton id={item.id} count={item.voteCount} active={item.votedByMe}/><div className="card-main"><div className="meta"><span className="category-dot" style={{background:item.category.color}}/>{item.category.name}<span>·</span><span className={`status ${item.status.toLowerCase()}`}>{item.status}</span></div><Link className="card-title" to={`/discussions/${item.id}`}>{item.title}</Link><p>{item.body.slice(0,180)}{item.body.length>180?'…':''}</p><footer><span className="avatar">{item.author.displayName?.[0]||item.author.username[0]}</span><Link to={`/users/${item.author.username}`}>{item.author.displayName||item.author.username}</Link><span>обновлено {new Date(item.updatedAt).toLocaleDateString('ru')}</span><span className="comments">◯ {item.commentCount}</span></footer></div></article>}
